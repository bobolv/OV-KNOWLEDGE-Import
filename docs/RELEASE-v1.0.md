# 工作记录入库平台-v1.0-手动导入

封版日期：2026-09-25。此版本为后续定时知识采集功能开发前的基线。

范围：人工上传、可选补录、批量导入、原件保留、WPS/ET转换文件关联、OV异步状态查询、资料说明同步、失败及结果未知处理、Docker部署与Tailscale绑定。

验证：7项自动化测试通过；既有浏览器验证记录见verification.json。封版不声称所有格式已在真实OV逐一验证。

不包含：Codex定时采集接口、自动摘要及审核队列。工作台密码、OV密钥、SQLite和业务原件均不进入Git。

回退：检出此标签，在本地保留.env及外部数据卷，运行docker compose up -d --build。数据库结构向后兼容，但新版本记录应在回退前备份。
